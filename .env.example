# Claude Code Usage Tracker - Docker Compose Configuration

# Server Authentication (REQUIRED - server will not start without this)
# Generate a secure secret with: openssl rand -base64 32
SESSION_SECRET=

# Grafana Admin Password (change in production!)
GRAFANA_PASSWORD=admin
